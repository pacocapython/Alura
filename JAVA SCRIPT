VARIAVEIS
const = declara variaveis, valor nao pode ser alterado apos ser declarado

TIPOS DE DADOS
Tipos Primitivos
Number = 
String = 
Boolean =
NODE.JS g
OPERADORES
FUNÇÕES
console.log = exibe resultado e mensagem na tela
EX :
const meuNumero = 1;
const meuNumero1 = 2;
const operaçãoMatematica = meuNumero / meuNumero1 
console.log(operaçãoMatematica) 
contatenação = somar strings
toLowerCase() que converte todos os caracteres da string informada (no caso, input) para letras minúsculas 
const ciidade = "sp";
const cidade = "Sp";
const cidadeMinusc = cidade.toLowerCase();
console.log(ciidade === cidadeMinusc);

A propriedade length pode ser utilizada para sabermos quantos caracteres uma string contém:
= atribui valor a uma variavel 
== = compara o valor, sem comparar tipos
=== = compara o valor e o tipo
-------------------------------------------------------------------------------------------------
var:

O que é: A forma mais antiga de declarar variáveis.
Características: Muito flexível, pode ser usada antes de ser declarada e permite redeclarações.
Quando usar: Evite usar em códigos novos. Pode causar bugs e dificultar o controle do código.
let:

O que é: Introduzido para resolver problemas do var.
Características: A variável precisa ser declarada antes de ser usada. Seu valor pode ser alterado (reatribuído) depois da declaração. Tem escopo de bloco (funciona dentro de {}).
Quando usar: Use quando você sabe que o valor da variável precisará mudar ao longo do programa.
const:

O que é: Introduzido para declarar constantes.
Características: A variável precisa ser declarada e inicializada com um valor no mesmo momento. Seu valor não pode ser alterado (é fixo) depois da declaração. Tem escopo de bloco.
Quando usar: Use para valores que nunca devem mudar no seu programa. É a opção mais segura e recomendada para a maioria dos casos.
Em poucas palavras:

const: Para valores que não mudam.
let: Para valores que podem mudar.
var: Não use em novos projetos.
-------------------------------------
if e else que nem em python
0 = false
1 = true
undefined = variavel que nao foi especificada, ela foi guardada, mas nao tem nenhum valor atribuido
null = null é basicamente como o undefined, mas neste caso a variavel foi definida, e sua definição é null(vazzio)
typeof = é uma palavra chave que serve para pergunta pro js qual o tipo de dado que está sendo guardado nesta variavel
// null em type of retorna object, é tipo um bug do javascript
let number = 1;
console.log(typeof number)

Truthy e Falsy:
O que é: O JavaScript consegue avaliar outros tipos de dados (números, strings, null) dentro de um if como se fossem verdadeiros ou falsos.

Falsy (Valores que o JS avalia como false):
false
0 (número zero)
"" (string vazia)
null
undefined
NaN (Not a Number)

Truthy (Valores que o JS avalia como true):
Todo o resto (qualquer número diferente de 0, strings preenchidas, etc.)
Exemplo prático:
let moedas = 0;
if (moedas) {
console.log("Tem moedas");
} else {
console.log("Não tem moedas"); // Vai cair aqui porque 0 é falsy
}

CONVERSÃO DE TIPOS E CONCATENAÇÃO

O JavaScript permite converter dados de um tipo para outro. Isso pode acontecer de forma automática (implícita) ou de forma manual (explícita).
Conversão Implícita (O JS faz sozinho):
Acontece quando você tenta fazer operações com tipos diferentes.
Ex: Se você somar um número com uma string, o JS converte o número para string e junta os dois.

Conversão Explícita (Você força a mudança):
Você usa funções nativas para transformar o tipo do dado de propósito.
Number() = Transforma uma string em número.
String() = Transforma um número em string.

Exemplo prático:
const numero = 456;
const numeroString = "456";

console.log(numero == numeroString);  // true (o JS converteu o tipo sozinho para comparar)
console.log(numero === numeroString); // false (tipos diferentes: Number e String)
console.log(numero + Number(numeroString)); // 912 (forçou a string a virar número e somou)

Concatenação:
O que é: É o ato de juntar (somar) duas ou mais strings para formar um texto único. O operador usado é o sinal de mais (+).
Exemplo prático:
const nome = "Gabriel";
const cargo = "Guerreiro";

const mensagem = "O " + nome + " é um " + cargo + ".";
console.log(mensagem); // Resultado: O Gabriel é um Guerreiro.

Cuidado com o sinal de +:
Number + Number = Soma matemática (2 + 2 = 4)
String + String = Concatenação ("2" + "2" = "22")
String + Number = Concatenação ("2" + 2 = "22")

EXEMPLO = NaN
let meuNome = "leonardo";
console.log(Number(meuNome)); // como a variável meuNome não contém apenas números ele retorna o erro NaN (Not a Number, não é número);
console.log( + meuNome); // a conversão também retornará NaN

O que é o JavaScript?
Tipagem dinâmica
- Não precisa declarar o tipo da variavel, ele ja entende sozinha
Multiparadigma
- Tem diversas formas de resolver diversos problemas
Por que se chama JS?
- Bla bla bla
O que é ES6 / ES2015?
- Atualização de correção de bugs
Linguagem interpretada
- LINGUAGEM INTERPRETADA VS COMPILADA
O computador só entende 0 e 1. Para o seu código rodar, ele precisa ser traduzido:
Interpretada (Ex: JavaScript, Python): O código é traduzido e executado linha por linha, em tempo real. É mais rápida para testar, mas pode ser um pouco mais lenta para tarefas super pesadas.
Compilada (Ex: C++, Rust): O código inteiro é traduzido de uma vez só antes de rodar, gerando um arquivo final (tipo um .exe). É extremamente rápida, mas toda alteração exige traduzir tudo de novo.


















